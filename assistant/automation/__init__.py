"""Scripted and agent-driven actions inside apps.

    desktop.py   Windows UI Automation (pywinauto): find windows, list and click controls
    browser.py   Playwright driving your installed Edge over CDP (the window stays open)
    macros.py    TOML automations in automations/: run instantly by voice, shown on the canvas
    __main__.py  `nova-cli ui ...` and `nova-cli macro ...`
"""
