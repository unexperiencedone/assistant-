.env
data/
__pycache__/
*.pyc
canvas/node_modules/
build/
dist/
.playwright-mcp/

# Not ignored by default and it held a live token once: keep credentials in .env only.
env
debug_screenshots/

# Page snapshots and scratch dumps agents leave in the root while working.
/contact.yml
/*.snapshot.yml
/snapshot*.yml
